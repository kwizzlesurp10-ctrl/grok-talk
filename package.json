{
  "name": "fusionpanda-master",
  "version": "4.2.2",
  "description": "FusionPanda Master — static fusion game: lab, collection, codex, arena. Build: npm run build",
  "private": true,
  "scripts": {
    "start": "npx -y serve -l 3000 .",
    "build:html": "node strip.cjs",
    "build": "npm run build:html && npm test",
    "test": "node tests/build.test.cjs"
  }
}
